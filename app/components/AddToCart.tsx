"use client";

const AddToCart = () => {
  return (
    <div>
      <button className="btn btn-primary" onClick={() => console.log("Hello")}>
        Add to Cart
      </button>
    </div>
  );
};

export default AddToCart;
